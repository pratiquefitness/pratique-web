import { theme as antheme } from 'antd'

const theme = {
  red: {
    algorithm: antheme.darkAlgorithm,
    token: {
      colorPrimary: '#ed143d',
      controlHeight: 50
    }
  },
  green: {
    algorithm: antheme.darkAlgorithm,
    token: {
      colorPrimary: '#acc571',
      controlHeight: 50
    }
  },
  gold: {
    algorithm: antheme.darkAlgorithm,
    token: {
      colorPrimary: '#A2722A',
      controlHeight: 50
    }
  }
}

export default theme
